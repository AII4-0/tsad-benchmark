name: TranAD-NASA-SMAP

entrypoint: python main.py --determined --config benchmarks/TranAD_NASA-SMAP.yaml

searcher:
  name: single
  metric: f1_adjusted
  max_length: 1

checkpoint_storage:
  save_trial_latest: 1000

bind_mounts:
  - host_path: /data
    container_path: /run/determined/workdir/data


max_restarts: 0